# Python dependencies for the MediaPipe hand teleop node.
#   pip install -r requirements.txt
#
# Install into a Python 3.12 env so ROS 2 Jazzy's rclpy can be imported.
mediapipe==0.10.21
opencv-contrib-python<5
numpy<2
PyYAML
